#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

echo "-----> Building qrencode"

curl http://fukuchi.org/works/qrencode/qrencode-3.2.0.tar.gz | tar xzf
cd qrencode-3.2.0; ./configure; make; make install
cd $1

export PATH=\"\$HOME/qr/qrencode-3.2.0:$HOME/bin:/.bin:\$PATH\"
